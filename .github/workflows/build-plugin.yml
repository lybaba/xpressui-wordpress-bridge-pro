name: Build WordPress Plugin PRO

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Extract version from tag
        id: version
        run: echo "version=${GITHUB_REF_NAME#v}" >> "$GITHUB_OUTPUT"

      - name: Build plugin ZIP
        run: |
          ZIP_NAME="xpressui-wordpress-bridge-pro-${{ steps.version.outputs.version }}.zip"

          # Build from parent so the zip root is xpressui-wordpress-bridge-pro/
          cd ..
          zip -r "$ZIP_NAME" xpressui-wordpress-bridge-pro \
            --exclude "*/.git/*" \
            --exclude "*/.github/*" \
            --exclude "*/scripts/*" \
            --exclude "*/.distignore"
          mv "$ZIP_NAME" xpressui-wordpress-bridge-pro/

          echo "ZIP_NAME=${ZIP_NAME}" >> "$GITHUB_ENV"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          name: "v${{ steps.version.outputs.version }}"
          files: ${{ env.ZIP_NAME }}
          generate_release_notes: true
